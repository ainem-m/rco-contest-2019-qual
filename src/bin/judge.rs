const DR: [usize; 4] = [1, 0, !0, 0];
const DC: [usize; 4] = [0, 1, 0, !0];
const REMOVED: usize = 0;

struct TestCase {
    N: usize,
    M: usize,
    A: Vec<Vec<usize>>,
}

impl TestCase {
    const N_FIXED: usize = 50;
    const M_FIXED: usize = 2500;
    const MIN_A: usize = 1;
    const MAX_A: usize = 9;
    fn new()
}

fn main() {}
